Install GNU C Library in ubuntu
~$ sudo apt-get update
~$ sudo apt-get install glibc-source
Check version of glibc
~$ldd --version
Install Open-mpi / mpich in ubuntu
~$ sudo apt-get install libopenmpi-dev openmpi-bin libhdf5-openmpi-de
Check version of open-mpi
~$ mpicc -v